<h1>My Blog</h1>

<ul>
  <% @posts.each do |post| %>
    <li><%= link_to post.title, post_path(post), :class => 'btn btn-skill' %><br><br></li>
    <% end %>
</ul>
<% if current_user.admin %>
<p><%=link_to "New Post", new_post_path, :class => 'btn btn-info'%></p>
<% end %>
